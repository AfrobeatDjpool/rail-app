<%= react_component('songs/SongsComponent', {songs: @songs, current_user: current_user}) %>
<div class="digg_pagination">
	<div class="page_info">
	  <%= page_entries_info @songs %>
	</div>
	<%= will_paginate @songs, :container => false %>
</div>